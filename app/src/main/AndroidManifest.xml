<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.katenumbers.dogbreedsquiz">

    <application
        android:name=".DogApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/dog_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dog_launcher_round"
        android:supportsRtl="true"
        android:windowSplashScreenBackground="#1d73ee"
        android:theme="@style/Theme.DogBreedsQuiz"

        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>